# Brain Tumor Classifier
A PyTorch-based deep learning model for classifying brain tumors from MRI scans with Grad-CAM explainability and a Gradio-based interactive demo.

![Python](https://img.shields.io/badge/Python-3.12-blue)
![PyTorch](https://img.shields.io/badge/PyTorch-Model-red)
![Project](https://img.shields.io/badge/Type-DeepLearning-green)

This project was built as a learning exercise in deep learning and model interpretability.

---

## Overview

This project fine-tunes a pretrained ResNet18 model to classify brain MRI scans into four categories. It also implements Grad-CAM to visually explain which regions of the scan the model focuses on when making a prediction, exposed through an interactive Gradio web interface.

**Classes:**
- Glioma
- Meningioma
- No tumor
- Pituitary

---

## Features

- Transfer learning with ResNet18 (ImageNet pretrained)
- Grad-CAM heatmap visualization overlaid on MRI scans
- Augmentation options at inference time: blur, noise, black box occlusion
- Gradio web UI for interactive inference
- EDA notebook section with class distribution, sample images, and pixel statistics
- Automatic dataset download via `kagglehub`

---

## Project structure

```
brain-tumor-classifier/
├── brain_tumor_classifier.ipynb   # Single notebook — full pipeline
├── models/
│   └── tumor_classifier.pth       # Saved model weights (generated after training)
├── dataset_split/                 # Auto-generated by preprocessing
│   ├── train/
│   ├── val/
│   └── test/
└── README.md
```

---

## Dataset

The dataset is downloaded automatically from Kaggle using `kagglehub`:

```python
import kagglehub
path = kagglehub.dataset_download("ishans24/brain-tumor-dataset")
```

It contains MRI scans across four tumor classes, split 70/20/10 into train, validation, and test sets.

To use `kagglehub` you need a Kaggle account and your API credentials configured. See the [Kaggle API docs](https://www.kaggle.com/docs/api) for setup.

---

## Requirements

- Python 3.12
- ROCm-compatible PyTorch (AMD GPU) or standard PyTorch (CPU/CUDA)
- See below for full dependency list

```
torch
torchvision
opencv-python
numpy
pandas
scikit-learn
matplotlib
Pillow
gradio
kagglehub
split-folders
```
The torch and torchvision packages are not pinned in requirements.txt to allow users to install the correct version depending on their hardware setup.

Install PyTorch separately before installing the remaining dependencies.
### - CPU (default)
For CPU-only usage:
```bash
pip install torch torchvision
pip install -r requirements.txt
```

### - AMD ROCM
For AMD GPUs, install the ROCm-compatible [PyTorch build](https://rocm.docs.amd.com/projects/install-on-linux/en/latest/)

⚠️ ROCm is officially supported on Linux. WSL support is possible but experimental and may require additional configuration.

🐳 Docker / Dev Container (ROCm-ready)

This repository includes a Docker Dev Container configured for a full ROCm environment.

It is intended for Linux + AMD GPU setups and provides a fully preconfigured environment, meaning no manual installation of dependencies is required inside the container and everything is handled automatically.

⚠️ Note: The Docker image is large (~40GB) due to the full ROCm + ML stack preinstalled.

### - NVIDIA CUDA
If you have an NVIDIA GPU, install PyTorch with CUDA support from the [official guide](https://pytorch.org/get-started/locally/)
Then install the remaining dependencies:
```bash
pip install -r requirements.txt
```

## Usage

The entire pipeline lives in a single notebook: `brain_tumor_classifier.ipynb`.

Run the cells in order:

| Cell | Description |
|------|-------------|
| 1 | Imports |
| 2 | Configuration & dataset download |
| 3 | Transforms |
| 4 | Preprocessing — dataset split |
| 5 | EDA — class distribution, sample images, pixel stats |
| 6 | Data loaders |
| 7 | Model definition |
| 8 | Evaluation helper |
| 9 | Training |
| 10 | Testing |
| 11 | Grad-CAM |
| 12 | Gradio app |

---

## Model

ResNet18 pretrained on ImageNet, with the final fully connected layer replaced to output 4 classes.

| Setting | Value |
|---------|-------|
| Optimizer | Adam |
| Learning rate | 0.001 |
| Epochs | 20 |
| Batch size | 32 |
| Loss | CrossEntropyLoss |
| Input size | 224 × 224 |

Training augmentations include random resized crop, horizontal flip, rotation, color jitter, and random erasing.

---

## Grad-CAM

Grad-CAM hooks into the last convolutional layer (`layer4`) of ResNet18 and computes a weighted activation map indicating which spatial regions most influenced the prediction. The heatmap is upsampled and overlaid on the original MRI.

Augmentation options available at inference:

- **None** — original image
- **Blur** — Gaussian blur (radius 5)
- **Black box** — center third of the image occluded
- **Noise** — Gaussian noise (σ = 25)

These let you probe how sensitive the model is to different parts of the scan.

---

## Acknowledgements

- Dataset: [Brain Tumor Dataset](https://www.kaggle.com/datasets/ishans24/brain-tumor-dataset) by ishans24 on Kaggle
- Model: [ResNet18](https://arxiv.org/abs/1512.03385) — He et al., 2015
- Explainability: [Grad-CAM](https://arxiv.org/abs/1610.02391) — Selvaraju et al., 2016
